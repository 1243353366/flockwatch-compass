# Server
HOST=0.0.0.0
PORT=8787
# Optional comma-separated origins in addition to the request's forwarded host.
ALLOWED_ORIGINS=https://project-compass-advisor.onrender.com
# Human or office accountable for this authorization policy.
AUTHORIZATION_POLICY_OWNER=Application owner (1243353366)

# Optional AI-assisted narrative. The transparent recommendation engine works without these.
# Set AI_MODE=off to force local-only recommendations even when a key is present.
AI_MODE=off
AI_API_KEY=
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini

# Private background worker only. Never expose this in browser code.
QUEUE_URL=
WORKER_CONCURRENCY=2
