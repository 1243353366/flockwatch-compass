{
  "name": "project-compass",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted project delivery methodology decision-support advisor.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/recommendation-engine.js && node --check public/app.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  }
}
